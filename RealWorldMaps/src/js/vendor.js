const L = require('leaflet'); 
const LBingLayer = require('leaflet-bing-layer');
const LSearch = require('leaflet-geosearch');
const LProviders = require('leaflet-providers');
 
let app = {
	L,
	LSearch
}

module.exports = app; 